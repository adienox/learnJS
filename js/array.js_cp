const array = [];

for(let i = 0; i < 10; i++){
  array[i] = Number(prompt('Enter a number'));
}

// remove empty index from array
//const filtered = array.filter(e =>  e);

const splits = array.split(e < 10);

/*for(let j = 0; j < filtered.length; j++){
  document.write(filtered[j]);
  //print comma only if there is following element is present
  if(j < filtered.length-1){
    document.write(",");
  }
}*/
//same as above
document.write(filtered);
